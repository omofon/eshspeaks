// Retired.
//
// The favicon set (public/favicon.ico, favicon-16x16.png, favicon-32x32.png,
// apple-touch-icon.png, android-chrome-192x192.png, android-chrome-512x512.png)
// is now maintained as real image files. They are regenerated from
// public/eshspeaks_logo.png with an external favicon generator such as
// realfavicongenerator.net and committed directly, which keeps the brand mark,
// colour profile and PNG compression that this file's pure-geometry rasteriser
// could not reproduce.
//
// To refresh them: feed the current public/eshspeaks_logo.png to the generator,
// drop its output into public/, then keep public/site.webmanifest and the
// `icons` block in src/app/layout.tsx in sync if any filenames change.

console.log(
  "generate-favicon is retired. Regenerate favicons from public/eshspeaks_logo.png " +
    "with an external favicon generator and commit the files directly.",
);
